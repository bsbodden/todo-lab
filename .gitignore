# Gradle / Kotlin
build/
.gradle/
.kotlin/
local.properties
kotlin-js-store/
# keep the wrapper jar even when a global gitignore has `*.jar` (else ./gradlew can't bootstrap in CI)
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.fleet/

# Xcode (the .xcodeproj is generated by XcodeGen in CI — don't commit it)
iosApp/iosApp.xcodeproj/
iosApp/build/
**/xcuserdata/
.DS_Store
